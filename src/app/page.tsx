import Link from "next/link";
import PulseCard from "@/components/PulseCard";
import { getSongs } from "@/lib/music";
import { keyNumbers } from "@/lib/pulse";
import { getCollectionEntries } from "@/lib/content";
export const dynamic="force-dynamic";
export default async function HomePage(){const [songs,articles]=await Promise.all([getSongs(),getCollectionEntries("articles")]);const top=songs.slice(0,5);const riser=[...songs].sort((a,b)=>(b.rank_change??-999)-(a.rank_change??-999))[0];const regional=songs.find(s=>(s.region??"").toLowerCase().includes("south"));return <main><section className="border-b border-black/10 bg-[#f5f3ee] dark:border-white/10 dark:bg-[#0b0b0d]"><div className="mx-auto max-w-[1400px] px-5 py-8 sm:px-8 lg:px-12"><div className="flex items-center justify-between text-[11px] font-bold uppercase tracking-[0.2em]"><span>The Sound Report</span><span>India · Music Intelligence</span></div><div className="py-24 sm:py-32 lg:py-40"><p className="mb-6 font-mono text-xs uppercase tracking-[0.2em] text-neutral-500">Updated daily · September 2026</p><h1 className="max-w-6xl font-display text-[clamp(4rem,11vw,10rem)] font-black uppercase leading-[.78] tracking-[-.07em]">India&apos;s<br/><span className="text-[#ff4d2e]">Music Pulse.</span></h1><div className="mt-10 flex max-w-2xl flex-col gap-5 sm:flex-row sm:items-end sm:justify-between"><p className="text-lg leading-relaxed text-neutral-600 dark:text-neutral-300">The data, artists and stories shaping how India listens. One place for the signal behind the noise.</p><Link href="/trends" className="shrink-0 border border-black bg-black px-6 py-3 text-center text-sm font-bold text-white transition hover:bg-[#ff4d2e] hover:border-[#ff4d2e] dark:border-white dark:bg-white dark:text-black">Explore the pulse →</Link></div></div></div></section><section className="mx-auto max-w-[1400px] px-5 py-10 sm:px-8 lg:px-12"><div className="grid border-l border-t border-black/10 dark:border-white/10 sm:grid-cols-2 lg:grid-cols-4">{[["#1 SONG",top[0]?.title??"Awaiting data",top[0]?.artist?.name??"Verified chart data required"],["FASTEST RISER",riser?.title??"Awaiting data",riser?.rank_change?`↑ ${riser.rank_change} places`:"Verified movement data required"],["BIGGEST YOUTUBE MOMENT","Awaiting signal","Official YouTube metrics required"],["REGIONAL BREAKOUT",regional?.title??"Awaiting data",regional?`${regional.language??"Regional"} · ${regional.region??"South India"}`:"Verified regional data required"]].map(([label,value,note])=><div key={label} className="border-b border-r border-black/10 p-6 dark:border-white/10"><p className="font-mono text-[10px] font-bold tracking-[0.18em] text-neutral-500">{label}</p><p className="mt-8 font-display text-3xl font-black uppercase leading-none">{value}</p><p className="mt-3 text-sm text-neutral-500">{note}</p></div>)}</div></section><section className="mx-auto max-w-[1400px] px-5 py-14 sm:px-8 lg:px-12"><div className="mb-7 flex items-end justify-between border-b border-black pb-4 dark:border-white"><div><p className="font-mono text-[10px] uppercase tracking-[0.2em] text-neutral-500">Live ranking</p><h2 className="mt-2 font-display text-4xl font-black uppercase">The Pulse</h2></div><span className="font-mono text-xs text-neutral-500">TSR SCORE / 100</span></div>{top.length===0?<div className="card-surface p-12 text-center text-sm text-neutral-500">The ranking is waiting for verified song and score data.</div>:<div className="grid gap-4 lg:grid-cols-3">{top.slice(0,3).map((song,i)=><PulseCard key={song.id} song={{id:song.slug,title:song.title,artist:song.artist?.name??"Artist pending",language:song.language??"",region:song.region??"",genre:song.genre??"",cover:song.cover_image_url??"",score:song.score??0,label:i===0?"#1":"Top ranking",reason:song.editorial_note??"Verified TSR ranking"}} featured={i===0}/>)}</div>}</section><section className="bg-black py-14 text-white dark:bg-white dark:text-black"><div className="mx-auto max-w-[1400px] px-5 sm:px-8 lg:px-12"><p className="font-mono text-[10px] uppercase tracking-[0.2em] opacity-50">Verified industry numbers</p><h2 className="mt-2 font-display text-4xl font-black uppercase">India by the numbers</h2><div className="mt-8 grid border-l border-white/20 dark:border-black/20 sm:grid-cols-2 lg:grid-cols-4">{keyNumbers.map(item=><div key={item.value} className="border-b border-r border-white/20 p-6 dark:border-black/20"><p className="font-display text-5xl font-black">{item.value}</p><p className="mt-3 max-w-[18rem] text-sm opacity-60">{item.label}</p></div>)}</div></div></section><section className="mx-auto max-w-[1400px] px-5 py-14 sm:px-8 lg:px-12"><div className="grid gap-10 lg:grid-cols-[1.2fr_.8fr]"><div><p className="font-mono text-[10px] uppercase tracking-[0.2em] text-neutral-500">Editorial</p><h2 className="mt-2 max-w-3xl font-display text-5xl font-black uppercase leading-[.9]">The Indian music economy is getting bigger than Bollywood.</h2><p className="mt-6 max-w-2xl text-lg leading-relaxed text-neutral-600 dark:text-neutral-300">Independent pop, regional languages and global discovery are changing the shape of the market. We follow the numbers, then tell you what they mean.</p><Link href="/journal" className="mt-8 inline-block border-b-2 border-[#ff4d2e] pb-1 text-sm font-bold uppercase tracking-wider">Read the analysis →</Link></div><div className="border border-black/10 p-6 dark:border-white/10"><p className="font-mono text-[10px] uppercase tracking-[0.2em] text-neutral-500">Latest journal</p>{articles.slice(0,4).map((a,i)=><Link key={a.frontmatter.slug} href={`/articles/${a.frontmatter.slug}`} className="flex items-center justify-between border-b border-black/10 py-5 last:border-0 dark:border-white/10"><div><p className="font-bold uppercase">{a.frontmatter.title}</p><p className="text-sm text-neutral-500">{a.frontmatter.excerpt}</p></div><span className="font-mono text-sm">0{i+1}</span></Link>)}</div></div></section><section className="border-t border-black/10 px-5 py-16 text-center dark:border-white/10 sm:px-8"><p className="font-mono text-[10px] uppercase tracking-[0.2em] text-neutral-500">The Sound Report</p><h2 className="mx-auto mt-3 max-w-3xl font-display text-4xl font-black uppercase">Follow the sound. Understand the market.</h2><p className="mx-auto mt-4 max-w-xl text-neutral-500">Weekly picks, artist intelligence, trend reports and the data behind India&apos;s music culture.</p></section></main>}
