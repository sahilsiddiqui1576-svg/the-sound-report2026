import Link from "next/link";
import type { Metadata } from "next";
import { getLatestCharts } from "@/lib/music";
export const dynamic="force-dynamic";
export const metadata:Metadata={title:"Charts",description:"Verified India music charts across Spotify, Apple Music, Amazon Music and YouTube."};
export default async function ChartsPage(){const charts=await getLatestCharts(50);const groups=charts.reduce<Record<string,typeof charts>>((m,c)=>(m[c.platform]=[...(m[c.platform]??[]),c],m),{});return <div className="container-editorial py-12 sm:py-16"><header className="border-b border-black pb-8 dark:border-white"><p className="font-mono text-[10px] font-bold uppercase tracking-[0.2em] text-neutral-500">Platform intelligence</p><h1 className="mt-3 font-display text-5xl font-black uppercase leading-none sm:text-7xl">Charts</h1><p className="mt-4 max-w-2xl text-neutral-500 dark:text-neutral-400">Verified chart snapshots from the platforms that define India&apos;s listening economy.</p></header>{charts.length===0?<div className="py-20 text-center"><p className="font-display text-3xl font-black uppercase">Charts are ready for data</p><p className="mt-3 text-sm text-neutral-500">No verified chart snapshots have been imported yet.</p></div>:<div className="mt-10 space-y-12">{Object.entries(groups).map(([platform,items])=><section key={platform}><div className="mb-4 flex items-end justify-between border-b border-black pb-3 dark:border-white"><h2 className="font-display text-3xl font-black uppercase">{platform}</h2><span className="font-mono text-xs text-neutral-500">{items.length} snapshots</span></div><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{items.map(c=><Link key={c.id} href={`/charts/${c.id}`} className="card-surface p-5 transition hover:-translate-y-0.5 hover:border-accent"><span className="font-mono text-[10px] uppercase tracking-wider text-neutral-500">{c.chart_date}</span><h3 className="mt-2 font-display text-2xl font-black uppercase">{c.chart_name}</h3><p className="mt-2 text-sm text-neutral-500">{[c.language,c.region,c.country].filter(Boolean).join(" · ")}</p></Link>)}</div></section>)}</div>}</div>}
