import Link from "next/link";
import type { Metadata } from "next";
import { getCollectionEntries } from "@/lib/content";
export const metadata:Metadata={title:"Journal",description:"Reporting, analysis and commentary from The Sound Report."};
export default async function JournalPage(){const entries=await getCollectionEntries("articles");return <div className="container-editorial py-12 sm:py-16"><header className="border-b border-black pb-8 dark:border-white"><p className="font-mono text-[10px] font-bold uppercase tracking-[0.2em] text-neutral-500">The journal</p><h1 className="mt-3 font-display text-5xl font-black uppercase sm:text-7xl">Journal</h1><p className="mt-4 max-w-2xl text-neutral-500 dark:text-neutral-400">Reporting and analysis on how India listens, discovers, exports and builds careers in music.</p></header><div className="mt-10 grid gap-5 md:grid-cols-2">{entries.map(e=><Link key={e.frontmatter.slug} href={`/articles/${e.frontmatter.slug}`} className="card-surface p-6 transition hover:-translate-y-0.5 hover:border-accent"><p className="font-mono text-[10px] uppercase text-neutral-500">{e.frontmatter.publishDate} · {e.readingTimeMinutes} min read</p><h2 className="mt-3 font-display text-3xl font-black uppercase">{e.frontmatter.title}</h2><p className="mt-3 text-sm leading-6 text-neutral-500">{e.frontmatter.excerpt}</p></Link>)}</div></div>}
