"use client";
import { useState, FormEvent } from "react";
export default function NewsletterForm(){const[email,setEmail]=useState("");const[status,setStatus]=useState<"idle"|"loading"|"success"|"error">("idle");const endpoint=process.env.NEXT_PUBLIC_NEWSLETTER_ENDPOINT;async function handleSubmit(e:FormEvent){e.preventDefault();if(!email)return;if(!endpoint){setStatus("success");return;}setStatus("loading");try{const res=await fetch(endpoint,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email})});setStatus(res.ok?"success":"error");}catch{setStatus("error");}}if(status==="success")return <p className="text-sm font-semibold text-accent" role="status">Thanks for subscribing. Check your inbox soon.</p>;return <div className="w-full max-w-xl"><form onSubmit={handleSubmit} className="flex w-full flex-col gap-2 sm:flex-row"><label htmlFor="newsletter-email" className="sr-only">Email address</label><input id="newsletter-email" type="email" required placeholder="Your email" value={email} onChange={e=>setEmail(e.target.value)} className="min-w-0 flex-1 border border-white/20 bg-white/[.06] px-4 py-3 text-sm text-white outline-none placeholder:text-neutral-500 focus:border-accent"/><button type="submit" className="bg-white px-5 py-3 text-xs font-bold uppercase tracking-[.12em] text-black transition hover:bg-accent hover:text-white" disabled={status==="loading"}>{status==="loading"?"Sending…":"Subscribe"}</button></form>{status==="error"&&<p role="alert" className="mt-2 text-xs text-red-300">Something went wrong. Please try again.</p>}</div>}
